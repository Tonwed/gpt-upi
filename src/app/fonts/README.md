# Local fonts

This directory contains self-hosted font files used by `src/app/layout.tsx` via `next/font/local` so production builds do not depend on fetching Google Fonts at build time.

Before publishing or redistributing, verify the license of any font files you add here.
